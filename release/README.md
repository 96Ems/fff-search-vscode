# Releases

Versioned VSIX builds are stored by target platform:

- `release/<version>/win32-x64/`
- `release/<version>/linux-x64/`

The root-level `*.vsix` and `*.tgz` files remain local build artifacts and are ignored.
